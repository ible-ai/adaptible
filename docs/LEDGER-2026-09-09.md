# Adaptible remediation ledger

Status: [ ] todo  [~] in progress  [x] done  [-] deferred (needs long compute / user call)

## Env
- [x] `.venv` (py3.13, mlx/mlx-lm/fastapi/vizible/absl/ddgs; no torch/lm-eval)
- [x] Baseline: 97 fast tests, 6 errors (uvicorn 0.36 `setup_event_loop`; stale tokenizer mock in revise_test)
- [x] `adaptible/_src/_paths.py` — single outputs-dir resolver

## Agent A — core runtime bugs (`_api.py`, `_llm.py`, `local/_server.py`, `tests/{api,llm,local,integration}_test.py`)
- [x] A1 `/trigger_review` never runs training (unawaited `asyncio.to_thread`) + test that fails without fix
- [x] A2 unreviewed indices never cleared after review
- [x] A3 assistant turns never appended to `_messages` + test
- [x] A4 dead `threading.Lock` in `__init__` and no-op lock in `/sync`; real guard between generate and train
- [x] A5 `MODEL_PATH` relative to CWD → use `_paths.default_checkpoint_path()`
- [x] A6 `llm_test` tautological try/except-pass; `integration_test` has zero assertions
- [x] A7 uvicorn 0.36 `setup_event_loop` removed → `local_test` errors
- [x] A8 `_detect_token_loop` dead branch; last chunk dropped on line-loop break

## Agent B — eval/meta (`eval/*.py`, `scripts/run_meta_experiment.py`, new `tests/eval_test.py`)
- [x] B1 `training_source` flag: `ground_truth` | `self_generated` (harness + meta + CLIs)
- [x] B2 `holdout_accuracy` computed then discarded in meta.py
- [x] B3 checkpoints cumulative → add per-window (marginal) metrics; score on windows
- [x] B4 `repeats` per seed + within-seed vs across-seed variance (control arm)
- [x] B5 `default_judge`/`contains_key_terms`: unicode-normalize (NFKC) before matching
- [x] B6 `total_net_learning` docstring lies; `final_accuracy` is trained-items-only → name it so
- [x] B7 harness/meta duplicate baseline/train/record phases → share helpers
- [x] B8 model-free tests via fake model

## Agent C — packaging, paths, autonomous (`db.py`, `pyproject.toml`, `.gitignore`, CI, `_src/static/`, `autonomous/*`)
- [x] C1 `DEFAULT_DB_PATH` resolves into site-packages → `_paths.default_db_path()`
- [x] C2 `.gitignore` (.venv, outputs/adaptible.db, checkpoints, __pycache__, reports)
- [x] C3 remove 6 redundant favicon PNGs (~14 MB) + `crop.sh`; keep one used by index.html
- [x] C4 move committed log out of package; node logs to `_paths.autonomous_log_dir()`
- [x] C5 `duckduckgo-search` → `ddgs`; drop unused heavy deps (torch, lm-eval, optax) if unused
- [x] C6 GitHub Action: fast test modules on macos runner
- [x] C7 autonomous: reject non-question claims, require answer grounded in snippet, no training on ungrounded "new knowledge" by default
- [x] C8 stale tokenizer mock in `revise_test` (`add_generation_prompt` kwarg)

## Agent D — docs (after A/B/C): README, analysis_report.md, sub-READMEs, CLAUDE.md
- [ ] D1 README: state that all published numbers are ground-truth SFT, not self-correction
- [ ] D2 analysis_report: conclusion → inconclusive at n=3, no control; keep observations, drop causal narrative
- [ ] D3 README project layout + log path + sub-README `_src/` layouts corrected
- [ ] D4 CLAUDE.md updated for new flags/paths

## Deferred (compute)
- [-] Run eval with `--training-source self_generated` (hours on M-series)
- [-] Run meta with `--repeats 3` on one seed (control arm, ~1 day)

## Found during remediation
- [x] E1 `python -m adaptible.{local,eval,autonomous}` all fail ("No module named") — README/CLAUDE.md advertise them; `adaptible/__init__.py` aliases modules, not packages. Fix: real `adaptible/{local,eval,autonomous}/` shim packages with `__main__.py` (after B finishes) — DONE: shim packages adaptible/{eval,local,autonomous,revise}
- [x] E2 `NodeState.load` crashed on the shipped state.json (legacy keys) — fixed by C
- [x] E3 autonomous `main()` ran every topic twice — fixed by C
- [x] Full fast suite: 161 tests OK (was 97 with 6 errors)

## Phase 2 — make the loop work (goal reframed 15:50: working loop, not auditing old results)
- [x] F1 transformers 5 BatchEncoding crash; mlx_lm cache-arg crash (be425b5, 9954eea)
- [x] F2 self_generated run #1: 1/84 valid revisions; 25-iter training collapses output style (eaa7f02)
- [x] F3 training target inside unclosed <think> → close_think (93435cd)
- [x] F4 default revision prompt embeds BOS/EOS mid-prompt → fewshot preset w/ plain dialog (93435cd)
- [x] F5 control arm killed after repeat 1 (holdout 12→6, answer collapse; DB exp 5); old-target arms dropped
- [~] F6 gt_close running (fixed target, ground truth, 5 iters) → sanity: no collapse, holdout ≥ baseline?
- [ ] F7 self_close queued (fixed target + fewshot, 5 iters) → valid-revision rate, net learning
- [~] F8 multi-turn training prefix drops prior turns (agent)
- [~] F9 instrument revision quality: valid / correct / fixed / broke (agent)
- [ ] F10 tune iterations/lr against collapse once F6/F7 land
- [x] F6 gt_close: trained 51→73/84 but holdout 12→6/21, every response empty-think + 2 words → format fix necessary, not sufficient
- [x] F11 think_mode=baseline + rehearsal_k (5ea6513); micro-batched after Metal OOM (a56b011)
- [x] F12 screens: lr5e-5×5 → garbage tokens; lr1e-5×3 → zero effect
- [x] F13 probe_lr: collapse is driven by loss→0, not lr. loss≈0.6 flips answer w/ reasoning intact; loss<0.1 collapses → adaptive stop (agent)
- [ ] F14 screen with loss_target 0.6, lr 2e-5, cap 12, rehearsal 4 → then full self_generated run
- [x] F14 screen_lt06 (lr2e-5): Metal OOM at item 6 → cache cap + clear_cache (f02b1d5); loss overshoots 3.2→0.15 in one step
- [x] F15 screen_lt06_lr5e6: lands loss 0.39 mean in 3.7 steps, but still collapses (718→21 tok, 36/40 empty-think, holdout 1→0/8). Rehearsal inert under loss target (own outputs already <0.6)
- [~] F16 joint objective grad(correction)+λ·grad(rehearsal) w/ accumulation; --lora_rank/--lora_layers/--lora_scale (agent)
- [ ] F17 probe: LoRA rank 8 / 8 layers dose-response (collateral damage per correction)
- [x] F16 joint objective committed (75ea265). Probe: rehearsal every step → NO collapse (all lengths normal, rehearsal+probe items intact) but correction doesn't take at loss 0.57 (reasoning still concludes Montreal)
- [x] F17 rank8/8-layer probe: finer control, unrelated item survives longer, but bare-answer collapse identical once flipped → capacity is second-order
- [~] F18 think_mode=rationale (reasoning consistent w/ correct answer, full-sequence target, stop on answer-token loss) — agent building; hand probe running
- [x] F18 hand probe: rationale target + rehearsal/step, rank8, lr1e-5 → Ottawa at 16 steps, reasoning 246 tok, probe+rehearsal intact. FIRST clean correction. Whole-seq loss 0.15 at flip → need answer-token stop (agent)
- [~] F19 second-item probe (geo_001) for step-count calibration
- [x] F19 geo_001 probe: Canberra at 4 steps, stable through 16, reasoning + probe intact. 2/2. Step count varies 4–16 → answer-token stop essential
- [ ] F20 screen_rationale: --think_mode rationale --lora_rank 8 --lora_layers 8 --learning_rate 1e-5 --loss_target 0.6 --iterations 20 --rehearsal_k 3 (after agent lands)
- [ ] F21 full self_generated run with the same recipe + fewshot prompt
- [x] F20 screen_rationale: still collapsed (718→32 tok, 36/40 empty-think). Causes: 13/32 rationales missing → fell back to empty-think target; rehearsal loss driven to 0.09 (pool ~12) → rehearsal itself overfits
- [~] F22 never train on empty-think fallback (use full output as rationale if no </think>, else skip); rehearsal hinge (gradient only when loss > initial+margin) (agent)
- [~] F23 probe why rationale prompt yields no </think> 40% of the time
- [x] F23 missing rationales = loop-detector-truncated rambling on the items the model is most confused about (geo_003: 803 tok about Brazil, no </think>); 4/5 well-formed
- [x] F22 screen_rationale2: NO COLLAPSE (516 tok, 0 empty-think, 0 missing). Net learning zero: 4/12 fixed, 4/20 regressed, holdout 1→0/8. Rehearsal active 1% → collapse was entirely the answer-only fallbacks
- [ ] F24 stuck classes: (i) cap-hit w/ answer loss 2–5 (geo_004, sci_006, geo_014) → more steps/lr; (ii) answer loss ≤0.6 but free-run still wrong (geo_013 0.05@1 step, geo_003, geo_019) → verify-by-generation loop
- [~] F25 skip baseline-correct items (60% of steps, source of interference) + verify-after-target loop (agent)
- [~] F26 probe geo_013 at lr 2e-5: does continued training flip a class-(ii) item?
- [x] F26 geo_013 probe (lr 2e-5): Manila at 4 steps, wrong at 8, right at 12/16 → class-(ii) items DO flip with continued training; verify-by-generation is the right stop
- [~] F25 agent hit session rate limit mid-task (00:00 reset); resumed 05:30 with partial harness.py edits in tree
