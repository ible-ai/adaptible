# Handoff, 2026-09-10 evening

Read this, then `docs/FINDINGS-2026-09-10.md`, then nothing else before
talking to the user.

## How the user works (this matters more than the code)

- Wants **minimum viable experiments**: one question, one script, one table,
  under an hour. Not machinery, not sweeps, not multi-pass runs "to find out
  the basic components don't work".
- Wants **cliffnotes updates**: lead with what happened and what is next,
  numbered, tables over prose, no recap, no preamble. They invoked the
  `i-have-adhd` skill for this; keep that shape.
- **Test their objections instead of arguing them away.** They predicted the
  token-edit chain reaction; I argued it would not happen; the probe proved
  them right and cost an afternoon.
- **Never run a useless experiment.** Kill anything whose config is known to
  be wrong. Probe on 1 to 5 items before any screen.
- Learning-rate tuning is "tuning the engine on a car that doesn't run".
  Do not sweep it again.
- Their long-term goal: a model that converges to a state where its own
  reflections are useful training signal for itself. They accept that
  fixes disturb other paths; the criterion is coherence plus an upward
  trajectory over iterations, not zero interference. That is later. Right
  now: prove one trace can be fixed so the fact is learned (paraphrases)
  and holds.

## Where things stand

- Recipe that keeps the model coherent (committed, `31d87b3`):
  `--think_mode rationale --lora_rank 8 --lora_layers 8 --learning_rate 2e-5
  --loss_target 0.6 --iterations 30 --verify_steps 4 --rehearsal_k 3`.
  Verify rounds halve the target to a 0.15 floor.
- The MVE (`scripts/probes/mve_paraphrase.py`, ~35 min) is the current
  yardstick: 5 wrong items, original + 3 paraphrases, before / after own
  training / after all 5. Result table in FINDINGS. All originals fixed and
  held; paraphrases 5/15 → 7/15; 5 of 20 final generations looped in the
  think block and never answered (3 at baseline).
- The continuous interference metric lives in `scripts/probes/probe_floor.py`
  (`measure()`): reuse it, do not reinvent it.
- Rehearsal as implemented (`active_rehearsal` hinge) has never activated.
  Any result that says "with rehearsal" had none.

## Next, as agreed with the user

1. Look at the coherence loss: why paraphrases of a trained item loop in the
   think block after training. Compare think length and loop rate before and
   after on the MVE items. Small script, one table.
2. Then the Australia pattern (training the original overwrote paraphrases
   the model already had). Candidate: include one paraphrase in the training
   target, or train on the model's own correct paraphrase output.
3. Then the iterative loop in the user's words: repair, generate, judge
   whether better, accept or reject (restore), continue. Graded judge (hit
   rate over k samples), external judge where possible. Weight restore must
   be from copies with a checksum (see `probe_pivot2.py`).

## Running things

- One model process at a time (16 GB). `pgrep -f "python outputs/runs/"`
  or `scripts/probes/` before launching.
- Probes: `PYTHONPATH=. .venv/bin/python scripts/probes/<name>.py`, with
  START/END markers appended to `outputs/runs/status.txt` by the launcher
  (`nohup sh -c '...; echo "[$(date)] END <name> rc=$?" >> outputs/runs/status.txt' &`).
  If a launcher is killed, write its END marker by hand.
- Probes read baseline outputs from `outputs/adaptible.db`, experiment 16
  (`screen_verify2`, the 40-item subset), so they do not regenerate them.
- 27 tok/s decode, ~700 tokens of thinking per trivia answer, so ~27 s per
  generation. Budget experiments in generations.
- `AGENTS.md` regenerates nightly at ~00:03 (Codex sync). User said delete it.
- Model-free tests: see `CLAUDE.md`. 260 pass as of `31d87b3`.

## Do not redo

- lr sweeps; EOS masking; token-level pivot edits; whole-path training with a
  tiny anchor weight; stop rules on a proxy instead of the generated answer;
  weight restore without a checksum. All documented in FINDINGS.
- Do not cite any number from before 2026-09-09 (ground-truth SFT on a
  malformed target) or any "rehearsal" result (inert).
